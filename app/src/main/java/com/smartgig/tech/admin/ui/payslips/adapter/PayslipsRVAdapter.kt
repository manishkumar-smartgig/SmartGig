package com.smartgig.tech.admin.ui.payslips.adapter

/*
class PayslipsRVAdapter(
    private val context: Context,
    private val payslipsData: List<PayslipsItem>,
//    private val onItemClick: (PayslipsItem) -> Unit
    ):RecyclerView.Adapter<PayslipsRVAdapter.ItemHolder>() {

    class ItemHolder(private val binding: PayslipRecyclerviewBinding) :
        RecyclerView.ViewHolder(binding.root) {
        val year: TextView = binding.tvYear
        val view: TextView = binding.tvView
        val download: ImageButton = binding.btDownload



    }

    override fun onCreateViewHolder(parent: ViewGroup, viewType: Int): ItemHolder {
        val binding =
            PayslipRecyclerviewBinding.inflate(LayoutInflater.from(parent.context), parent, false)
        return ItemHolder(binding)

    }

    override fun getItemCount(): Int {
        return payslipsData.size
    }*/

   /* override fun onBindViewHolder(holder: ItemHolder, position: Int) {
        val item = payslipsData[position]
        holder.year.text = item.year
        val view = "view"
        val mSpanning = SpannableString(view)
        mSpanning.setSpan(
            UnderlineSpan(), 0, mSpanning.length, 0
        )
        holder.view.text = mSpanning

        holder.view.setOnClickListener {

        }
        holder.download.setOnClickListener {

        }
    }*/
//}